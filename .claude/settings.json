{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'git commit'; then cargo test --quiet 2>&1 | tail -5 && cargo clippy --quiet -- -D warnings 2>&1 | tail -10; fi"
          }
        ]
      }
    ]
  }
}
